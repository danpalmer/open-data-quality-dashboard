<%= form_for(@score_criterium) do |f| %>
  <% if @score_criterium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@score_criterium.errors.count, "error") %> prohibited this score_criterium from being saved:</h2>

      <ul>
      <% @score_criterium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
